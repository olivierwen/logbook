<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <a class="navbar-brand" href="/"><span class="glyphicon glyphicon-list-alt"></span> Carnet de vol</a>
    <% if logged_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to new_flight_path do %>
          Déclarer un vol <span class="glyphicon glyphicon-check" />
        <% end %></li>
        <li class="active"><%= link_to pilot_path(:id => current_user.id) do %>
          <span class="badge"><%= "#{current_user.name}" %><span class="glyphicon glyphicon-wrench"></span></span>
        <% end %>
        </li>
        <li><%= link_to flights_path do %>
          Les vols <span class="badge"><%= Flight.count %></span>
        <% end %>
        </li>
        <li><%= link_to pilots_path do %>
          Les pilotes <span class="badge"><%= Pilot.count %></span>
        <% end %>
        </li>
      </ul>
      <div class="col-sm-3 col-md-3">
          <form class="navbar-form" role="search">
          <div class="input-group">
              <input type="text" class="form-control" placeholder="Rechercher" name="q">
              <div class="input-group-btn">
                  <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
              </div>
          </div>
          </form>
      </div>
      <div class="collpase navbar-collapse navbar-right">
        <%= link_to 'Se déconnecter', session_path, method: :delete, :class => "btn btn-warning" %>
      </div>
    <% else %>
      <div class="collpase navbar-collapse navbar-right">
        <%= link_to 'S \'enregistrer', new_pilot_path, {:style=>'color:#FFFFFF;', :class => "btn btn-info"} %>
        <%= link_to 'Se connecter', new_session_path, {:style=>'color:#FFFFFF;', :class => "btn btn-success"} %>
      </div>
    <% end %>
  </div>
</nav>