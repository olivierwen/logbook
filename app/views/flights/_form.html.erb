<%= form_with(model: flight, local: true) do |form| %>
  <div class="form-group">
    <% if flight.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

        <ul>
        <% flight.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :startplace %>
      <%= form.text_field :startplace, id: :flight_startplace, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :stopplace %>
      <%= form.text_field :stopplace, id: :flight_stopplace, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :description %>
      <%= form.text_field :description, id: :flight_description, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :duration %>
      <%= form.text_field :duration, id: :flight_duration, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :date %>
      <%= form.date_select :date, id: :flight_date, :class => "form-control" %>
    </div>

    <div class="field">
      <%= form.label :length %>
      <%= form.text_field :length, id: :flight_length, :class => "form-control" %>
    </div>

    <div class="actions">
      <%= form.submit 'Déclarer le vol', :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
