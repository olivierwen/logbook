<%= form_with(model: flight, local: true) do |form| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :startplace %>
    <%= form.text_field :startplace, id: :flight_startplace %>
  </div>

  <div class="field">
    <%= form.label :stopplace %>
    <%= form.text_field :password, id: :flight_stopplace %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :flight_description %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration, id: :flight_duration %>
  </div>

  <div class="field">
    <%= form.label :length %>
    <%= form.text_field :length, id: :flight_length %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
