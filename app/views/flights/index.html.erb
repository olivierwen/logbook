<p id="notice"><%= notice %></p>

<h1>Vols</h1>

<table class="table table-condensed">
  <thead>
    <tr>
      <th>Pilote</th>
      <th>Lieu de départ</th>
      <th>Lieu d'arrivée</th>
      <th>Description</th>
      <th>Durée</th>
      <th>Date</th>
      <th>Distance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @flights.each do |flight| %>
      <tr>
        <td><%= flight.pilot_id %></td>
        <td><%= flight.startplace %></td>
        <td><%= flight.stopplace %></td>
        <td><%= flight.description %></td>
        <td><%= flight.duration %></td>
        <td><%= flight.date %></td>
        <td><%= flight.length %></td>
        <td><%= link_to 'Show', flight_path(flight) %></td>
        <td><%= link_to 'Edit', edit_flight_path(flight) %></td>
        <td><%= link_to 'Destroy', flight, method: :delete, data: { confirm: 'Êtes-vous sûr ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'Déclarer un vol', new_flight_path, { :class => "btn btn-success"}%>
